server:
  port: 8080
  compression:
    enabled: true
    mime-types: application/json,application/xml,text/html,text/xml,text/plain
    min-response-size: 512

management:
  endpoint:
    health:
      show-details: always
    info:
      git:
        mode: full
  endpoints:
    web:
      exposure:
        include: "beans,flyway,health,info,scheduledtasks"

spring:
  main:
    lazy-initialization: false
  autoconfigure:
    exclude:
      - org.springframework.boot.autoconfigure.security.servlet.SecurityAutoConfiguration
      - org.springframework.boot.autoconfigure.security.servlet.UserDetailsServiceAutoConfiguration

  jackson:
    deserialization:
      FAIL_ON_UNKNOWN_PROPERTIES: false
      ACCEPT_EMPTY_STRING_AS_NULL_OBJECT: true
    mapper:
      ACCEPT_CASE_INSENSITIVE_ENUMS: true
      ACCEPT_CASE_INSENSITIVE_PROPERTIES: true
    default-property-inclusion: non_null

wutsi:
  client-id: wutsi-tracking
  application:
    name: wutsi-tracking
    jobs:
      daily-chat:
        cron: "0 0 3 * * *" # every day at 3AM
      daily-share:
        cron: "0 0 3 * * *" # every day at 3AM
      daily-visit:
        cron: "0 0 3 * * *" # every day at 3AM

      monthly-chat:
        cron: "0 0 5 * * *" # every day at 6AM
      monthly-share:
        cron: "0 0 5 * * *" # every day at 6AM
      monthly-visit:
        cron: "0 0 5 * * *" # every day at 6AM
  platform:
    security:
      type: jwt
    stream:
      name: ${wutsi.application.name}
      type: local
      subscriptions:
        - wutsi-store-bff   # Listen to product related events
        - wutsi-shell-bff   # Listen to navigation events
    tracing:
      client-id: ${wutsi.application.name}
      device-id-provider:
        type: header
    storage:
      type: local

logging:
  level:
    root: INFO
    com.wutsi.analytics.tracking: DEBUG
  pattern:
    console: "%d type=%level logger=%class{20} thread=%thread %m%n"
    file: "%d type=%level logger=%class{20} thread=%thread %m%n"


